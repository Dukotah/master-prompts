# Overseer Run Report — 2026-06-08

Two runs on this date. Run A (marina promo admin, ~19:00 UTC) is preserved below; Run B (apex-quant F2.3, ~22:00 UTC) is appended.

---

## Run A — ~19:00 UTC (written by earlier session)

**Target:** marina-booking-platform (DOY 159 mod 5 = 4 → apex fell through blocked → marina)  
**Branch pushed:** `overseer/2026-06-07` on marina (promo admin page)

### Reconcile (Run A)

| Repo | vs origin/main | In-flight |
|---|---|---|
| boots | synced | `qa-fixes-2026-06-07` +7 vs main |
| Websites | synced | main 4 ahead/1 behind origin |
| Duke | synced | none |
| marina | synced | `phase-3-golive` +10; `feat/finish-mvp-buildable` +6 |
| apex | synced | `feat/research-buildout` +10 |

### Built (Run A): Admin promo code management page (marina)

Files: `apps/admin/app/promos/actions.ts`, `page.tsx`, `PromoTable.tsx`, `CreatePromoDialog.tsx`,
`PromoRowActions.tsx`, `Sidebar.tsx` (+1 nav item). Commit `658e478` on `overseer/2026-06-07`.

Verification: `npx tsc --noEmit` → 0 errors; core 69/69 tests; build not run (Neon not available).

Merge note: `Sidebar.tsx` has trivial 3-line conflict with `phase-3-golive` (add all three nav items).

---

## Run B — ~22:00 UTC (this session)

**Rotation:** UTC DOY 158 mod 5 = 3 → marina-booking-platform  
**Fallback:** marina both buildable items blocked on `phase-3-golive` merge (owner action) → **apex-quant** (index 4)

### Reconcile (Run B)

| Repo | origin/main HEAD | Key in-flight branches |
|------|-----------------|----------------------|
| boots | `baf075a` | `overseer/2026-06-07`: 2 ahead, 4 behind main; `qa-fixes-2026-06-07`: merged (0 ahead) |
| Websites | `7985e2e` | none significant |
| Duke | `15381fd` | none significant |
| marina | `6ac5ed2` | `phase-3-golive` 24 ahead; `overseer/2026-06-07` 1 ahead; `feat/finish-mvp-buildable` 6 ahead |
| apex-quant | `7d522e9` | `feat/research-buildout` 22 ahead / 15 behind (diverged); `feat/risk-hardening` 1/40 (obsolete?); `feat/status-export` 1/40 (obsolete?) |

### Built (Run B): apex-quant F2.3 — Daily Heartbeat Alert

**Problem:** Quiet trading days (no trades/halt/quarantine/kill) were completely silent. Owner
had no confirmation the cron was running vs. broken.

**Solution:** Added a `meta` key-value table to `StateStore` (stdlib SQLite, zero new deps).
`_heartbeat_if_due` fires one `priority="min"` ntfy push per calendar day on quiet cycles,
deduplicated by `last_heartbeat_date` in the meta table. Second quiet run same day: silent.
`_notify_cycle` signature extended with `store: Optional[StateStore] = None`. `alerts_preview`
updated with `heartbeat_due: bool = True` parameter and `_heartbeat_due_for` read-only helper.

**Files changed:**
- `scripts/run_once.py` — `StateStore` meta table + `get_meta`/`set_meta`; `_HEARTBEAT_META_KEY`; `_heartbeat_if_due`; `_notify_cycle` store param; `run_once` passes store.
- `scripts/alerts_preview.py` — `preview_alert(heartbeat_due=True)` + `_heartbeat_due_for`.
- `tests/test_alerts_preview.py` — 5 new tests (heartbeat-due fires, already-sent silent, message content, no heartbeat on actionable, DB integration).
- `tests/test_run_once.py` — 5 new tests (meta round-trip, once-per-day dedup, resets new day, suppressed on traded, unconditional without store).
- `DECISIONS.md` + `TASKS.md` (F2.3 → done).

**Verification:**
```
ruff check:          ALL CLEAN (0 errors)
ruff format --check: ALL CLEAN
pytest:              2517 passed (was 2507), 94.83% coverage, Required 70% ✓
```

**Branch:** `overseer/2026-06-08` · **Commit:** `d6013a0`

---

## New Owner-Action Blockers (Run B)

No new blockers. Existing confirmed:

- **apex-quant `feat/risk-hardening` / `feat/status-export`**: each 1 ahead / 40 behind — almost certainly obsolete. Owner should close or delete.
- **apex-quant `feat/research-buildout`**: 22 ahead / 15 behind — major divergence, needs rebase/squash-merge decision before F3.3 can build on it cleanly.
- **marina both items blocked** on `phase-3-golive` merge — next overseer run should skip marina to apex-quant automatically (the fallback logic works).

## Next Buildable (updated)

| Project | Next item | Blocked? |
|---------|-----------|---------|
| boots | P1.7 loading/empty/error state guards on primary routes | No |
| Websites | Fresh CSV batch; more section variants from CodeStitch | No (needs content decision) |
| Duke | Inline estimate widget or comparison posts | No |
| marina | wizard→storefront click-through | YES — awaiting phase-3-golive merge |
| apex-quant | F3.3 allocation engine (backtest mode) OR coverage uplift on thin modules | No (F3.3 research-gated on W8 but buildable in backtest mode) |
